{
    "default": {},
    "size_ranges": [
        {
            "min": 1,
            "max": 10,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 50,
                "save_every_n_epochs": 25,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        },
        {
            "min": 11,
            "max": 20,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 50,
                "save_every_n_epochs": 25,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        },
        {
            "min": 21,
            "max": 30,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 50,
                "save_every_n_epochs": 25,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        },
        {
            "min": 31,
            "max": 80,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 50,
                "save_every_n_epochs": 25,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        },
        {
            "min": 81,
            "max": 1000,
            "config": {
                "train_batch_size": 1,
                "max_train_epochs": 50,
                "save_every_n_epochs": 25,
                "unet_lr": 1,
                "text_encoder_lr": 1
            }
        }
    ]
}